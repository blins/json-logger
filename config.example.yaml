endpoints:
  - name: std
    logger:
      type: stdout
      options:
        channel: STE
  - name: pg
    logger:
      type: postgres
      options:
        dsn: host=localhost port=5432 user=postgres password=example dbname=product_logs sslmode=disable
        table-name: prod_logs